name: calc_sdk
description: "Standalone calculator (keypad, chained operators, history, memory keys) shared by the manager and driver compositions."
version: 1.0.0
publish_to: 'none'
environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.5'
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  get_it: ^8.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
