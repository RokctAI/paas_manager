header:
  license:
    spdx-id: AGPL-3.0-only
    copyright-owner: ROKCT INTELLIGENCE (PTY) LTD
    # The literal header text license-eye matches against. WITHOUT this,
    # license-eye has no template to compare and matches nothing at all:
    # it ships header templates for Apache-2.0 and MPL-2.0 ONLY, so neither
    # MIT nor AGPL-3.0 can be resolved from the spdx-id alone and the check
    # scores `valid: 0, invalid: <everything>` regardless of what the files
    # actually carry. Supplying the text makes the check actually check.
    #
    # This repository is licensed under the GNU Affero General Public
    # License v3 (see the root LICENSE file). Keep this block, the root
    # LICENSE and the header in every source file in agreement.
    content: |
      Copyright (c) 2026 ROKCT INTELLIGENCE (PTY) LTD

      This program is free software: you can redistribute it and/or modify
      it under the terms of the GNU Affero General Public License as published
      by the Free Software Foundation, version 3.

      This program is distributed in the hope that it will be useful,
      but WITHOUT ANY WARRANTY; without even the implied warranty of
      MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
      GNU Affero General Public License for more details.

      You should have received a copy of the GNU Affero General Public License
      along with this program. If not, see <https://www.gnu.org/licenses/>.

  # license-eye ships no comment style for Dart; declare one so `header fix`
  # can stamp .dart files (check mode is comment-style agnostic).
  language:
    Dart:
      extensions:
        - '.dart'
      comment_style_id: DoubleSlash

  # `paths` previously read `- 'lib'`. This repository has no lib/ directory
  # - it is a composed app whose sources live in .rokct/cache and
  # integration_test - so that allowlist matched NOTHING and the check
  # scored `valid: 0, invalid: 0`, passing vacuously without ever reading a
  # file. The allowlist below is the fleet-standard set of source
  # extensions, which restores a check that actually checks. Every
  # paths-ignore entry the repository already had is preserved unchanged.
  paths:
    - '**/*.py'
    - '**/*.js'
    - '**/*.jsx'
    - '**/*.ts'
    - '**/*.tsx'
    - '**/*.dart'

  paths-ignore:
    # Generated code - regenerated by build_runner / auto_route, not hand-maintained
    - '**/*.freezed.dart'
    - '**/*.g.dart'
    - '**/*.gr.dart'
    # Platform project scaffolding and assets
    - 'android'
    - 'ios'
    - 'assets'
    # Agent-internal docs and state
    - '.rokct'
    # Lockfiles, docs, config
    - 'pubspec.lock'
    - '**/*.md'
    - '**/*.json'
    - '**/*.yaml'
    - '**/*.yml'
    - 'LICENSE'
    - '.gitignore'

  comment: on-failure
