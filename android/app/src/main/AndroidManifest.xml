<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- @sdk-android-permissions-start -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- @sdk-android-permissions-end -->
    <!-- HMS wave (hms_sdk): huawei_hmsavailability 6.13.0+300 declares
         minSdk 26 in its Android library while the fleet ships minSdk 24
         (flutter.minSdkVersion), which fails the manifest merge in every
         composed app build. Override is safe: hms_sdk's Dart DeviceServices
         wraps every HMS call in try/catch and fail-opens to GMS, so a device
         below the library's expectations degrades gracefully at runtime
         instead of failing. Inert when the HMS plugins are not in the build.
         (huawei_push declares minSdk 21 and needs no override.) -->
    <uses-sdk
        tools:overrideLibrary="com.huawei.hms.flutter.hmsavailability"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove"/>
   <!-- <uses-permission android:name="android.permission.CAMERA" /> -->
    <queries>
        <intent>
            <action
                android:name="android.intent.action.VIEW"/>
            <data
                android:scheme="sms"/>
        </intent>
        <intent>
            <action
                android:name="android.intent.action.VIEW"/>
            <data
                android:scheme="tel"/>
        </intent>
    </queries>
    <!-- Backup / device-transfer policy. allowBackup stays true so the app
         still restores, but res/xml/data_extraction_rules.xml (API 31+) and
         res/xml/backup_rules.xml (pre-31) exclude every credential-bearing
         store: the offline sqlite database, the plain SharedPreferences file
         and the flutter_secure_storage EncryptedSharedPreferences file (whose
         Keystore master key is not backed up, so a restored copy is
         undecryptable). tools:replace pins these against library manifests
         that declare their own android:allowBackup.

         backupAgent and fullBackupOnly go together and must stay together.
         RokctBackupAgent exists only so onRestoreFinished can flag that a
         restore happened, which is what triggers Restore Credentials
         retrieval (Android's docs are explicit that onRestore is the wrong
         hook, firing for key-value backups only). Declaring an agent
         WITHOUT fullBackupOnly="true" switches the app to key-value backup,
         where that agent's no-op callbacks would back up nothing and both
         XML rule files above would be ignored entirely. With
         fullBackupOnly="true" Auto Backup keeps running exactly as before
         and the agent only adds the restore-finished callback. -->
    <application
        android:name=".JuvoApplication"
        android:allowBackup="true"
        android:backupAgent=".RokctBackupAgent"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:replace="android:label,android:allowBackup,android:dataExtractionRules,android:fullBackupContent,android:backupAgent,android:fullBackupOnly">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key"/>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

<!--        <activity-->
<!--            android:name="com.yalantis.ucrop.UCropActivity"-->
<!--            android:screenOrientation="portrait"-->
<!--            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />-->

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

<meta-data
    android:name="com.google.firebase.messaging.default_notification_icon"
    android:resource="@drawable/notification_icon" />
<meta-data
    android:name="com.google.firebase.messaging.default_notification_channel_icon"
    android:resource="@drawable/notification_icon" />
<meta-data
    android:name="com.google.firebase.messaging.default_notification_color"
    android:resource="@color/custom_notification_color" />
           

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
